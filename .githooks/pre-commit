#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[BAGO pre-commit] synchronizing README truth projection..."
python backend/scripts/generate_readme_projection.py
git add README.md backend/contracts/readme_projection.v1.json
python backend/scripts/generate_readme_projection.py --check
echo "[BAGO pre-commit] README truth projection synchronized."
