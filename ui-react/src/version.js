// Fallback version label used until the backend reports the real bago_version.
// Never hardcode a specific release number here; the runtime is the source of truth.
export const DEFAULT_VERSION_LABEL = 'current'
