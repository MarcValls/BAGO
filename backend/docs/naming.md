# Naming convention

Canonical docs in this tree use lowercase `kebab-case` filenames in English.

Legacy uppercase or mixed-language names are deprecated and should not be used for new references.

If a doc is renamed again, update the canonical filename first and then update links in `docs/README.md` and the relevant tests.
