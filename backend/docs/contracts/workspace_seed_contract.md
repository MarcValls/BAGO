# Workspace Seed Contract

`workspace seed` defines how BAGO detects a workspace, creates `.gabo` when missing, and builds a working set with a receipt.
Path resolution and canonical roots are owned by `docs/contracts/resolver_contract.json`.

## Required Terms

- workspace seed
- `.gabo`
- depth 9
- working set
- receipt

## Current Rule

The seed contract is active, but the detailed historical bundle now lives in `docs/archive/contracts/workspace_seed_contract/`.
