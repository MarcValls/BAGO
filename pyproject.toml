[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bago"
version = "3.5.0b1"
description = "BAGO runtime"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["bago_core*"]

[project.scripts]
bago = "bago_core.cli:main"
