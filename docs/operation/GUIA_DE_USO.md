# Guia de uso

1. Arranca BAGO desde el launcher correspondiente al entorno.
2. Ejecuta `bago health` para ver la salud del pack.
3. Ejecuta `bago validate` para comprobar manifiesto y estado.
4. Usa `bago project init` y `bago project link` para memoria por proyecto.
