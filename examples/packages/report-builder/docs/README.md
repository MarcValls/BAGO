# Report builder

Builds a deterministic Markdown or JSON report from a title, optional summary, and a JSON list of sections.

Each section accepts `title` and `content`. The capability performs no filesystem or network access.
