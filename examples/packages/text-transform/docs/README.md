# Text transform

Local text transformation without network access. Choose a fixed operation in configuration and pass text as input.

The `replace` operation uses the `find` and `replacement` configuration fields.
