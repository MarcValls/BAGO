# File batch

Creates a read-only inventory below a selected directory. It requests `filesystem.read`, caps each run at 200 files, and never modifies source files.

Activation and every execution require explicit permission approval.
