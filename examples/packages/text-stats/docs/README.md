# Text stats

Minimal executable capability for validating the `bago.package/v1` lifecycle. It returns normalized text, word count, and character count.

Importing the package does not run it. Review and activate it before using the sample input.
