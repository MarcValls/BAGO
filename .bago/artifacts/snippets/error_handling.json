{
  "content": "def safe_dispatch(registry, cmd, fallback=None):\n    try:\n        handler = registry.get(cmd)\n        if handler is None:\n            return fallback() if fallback else None\n        return handler()\n    except Exception as e:\n        log_guardian(f'Dispatch KO: {e}')\n        return fallback() if fallback else None",
  "language": "python",
  "lines": 8,
  "tags": [
    "python",
    "error-handling",
    "robustness"
  ]
}